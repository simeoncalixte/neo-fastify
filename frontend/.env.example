VITE_API_BASE_URL=http://localhost:3001
# Example environment variables for the frontend (Vite)
# Prefix client-exposed variables with VITE_ so Vite will expose them to the app.

# URL of the Configure server used by the app (change to your local or prod URL)
VITE_CONFIGURE_SERVER_URL=http://localhost:6007

# Optional API key or token for the Configure server (leave blank locally)
VITE_CONFIGURE_SERVER_API_KEY=

# Example: enable feature flag for configure integration
VITE_CONFIGURE_ENABLE=true

# Note: copy this file to .env and update values for your environment.

# Base URL for the backend API used by live Storybook stories
# Example: VITE_API_BASE_URL=http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001
